---
import {
  resolveServiceBooking,
  type BookingMode,
} from '@/lib/booking';

interface Props {
  slug: string;
  bookingMode?: BookingMode;
  bookingUrl?: string;
  location: string;
  class?: string;
  label?: string;
  accessibleContext?: string;
}

const {
  slug,
  bookingMode,
  bookingUrl,
  location,
  class: className = 'btn-gold',
  label,
  accessibleContext,
} = Astro.props;
const action = resolveServiceBooking({ slug, bookingMode, bookingUrl }, location);
const visibleLabel = label ?? action.label;
---

<a
  href={action.href}
  class={className}
  aria-label={accessibleContext ? `${visibleLabel}: ${accessibleContext}` : undefined}
  target={action.target}
  rel={action.rel}
  {...action.analytics}
>
  {visibleLabel}
</a>

---
import BaseLayout from '@/layouts/BaseLayout.astro';
import SchemaMarkup from '@/components/SchemaMarkup.astro';
import { sanityFetch } from '@/lib/sanity';
import { ABOUT_PAGE_QUERY, PUBLIC_PROVIDERS_QUERY, type AboutPageContent, type PublicProviderProfile } from '@/lib/queries';
import { ABOUT_PAGE_FALLBACK, resolvePublicProviderProfiles } from '@/lib/aboutFallbacks';
import { breadcrumbList } from '@/lib/structuredData';
import { sbObjectId } from '@/lib/visualEditing';

const content = await sanityFetch<AboutPageContent | null>(ABOUT_PAGE_QUERY);
const sanityProviders = await sanityFetch<PublicProviderProfile[]>(PUBLIC_PROVIDERS_QUERY);
const providers = resolvePublicProviderProfiles(sanityProviders);
const sanityProviderIds = new Set(sanityProviders.map((provider) => provider._id));

// Working voice reset: public copy stays local while images and provider facts may remain in Sanity.
const heading = 'House of Rose Aesthetics.';
const intro = 'A medical aesthetics practice in Punta Gorda, with medical and esthetic services offered by people whose names, credentials, and work are easy to find.';
const imageUrl = content?.indexImageUrl ?? ABOUT_PAGE_FALLBACK.indexImageUrl;
const imageAlt = content?.indexImageAlt ?? ABOUT_PAGE_FALLBACK.indexImageAlt;
const title = ABOUT_PAGE_FALLBACK.indexSeo?.metaTitle;
const description = ABOUT_PAGE_FALLBACK.indexSeo?.metaDescription;

const site = Astro.site?.toString() ?? 'https://houseofrosefl.com/';
const canonical = new URL(Astro.url.pathname, site).toString();
const breadcrumb = breadcrumbList([
  { name: 'Home', item: site },
  { name: 'About', item: canonical },
]);
---

<BaseLayout
  title={title}
  description={description}
  image={imageUrl}
  imageAlt={imageAlt}
  preloadImage={imageUrl}
>
  <SchemaMarkup item={breadcrumb} slot="head" />

  <div {...(content?._id ? sbObjectId(content._id) : {})}>
    <section class="px-6 pb-[clamp(56px,8vw,96px)] pt-32">
      <div class="mx-auto grid max-w-[1280px] items-end gap-10 lg:grid-cols-[0.9fr_1.1fr]">
        <div class="max-w-[650px]">
          <p class="kicker">About</p>
          <h1 class="font-serif text-[clamp(42px,7vw,78px)] leading-[0.98] text-ink">
            {heading}
          </h1>
          <p class="mt-7 max-w-[610px] text-[17px] leading-[1.8] text-muted">
            {intro}
          </p>
        </div>
        <div class="aspect-[16/10] overflow-hidden bg-limestone">
          <img
            src={imageUrl}
            alt={imageAlt}
            width="1400"
            height="875"
            class="h-full w-full object-cover"
            fetchpriority="high"
          />
        </div>
      </div>
    </section>

    <section class="px-6 pb-[clamp(56px,8vw,96px)]">
      <div class="mx-auto grid max-w-[1080px] gap-8 border-t border-walnut/15 pt-[clamp(40px,6vw,68px)] md:grid-cols-[0.75fr_1.25fr] md:gap-16">
        <div>
          <p class="kicker">A local practice</p>
          <h2 class="font-serif text-[clamp(30px,4vw,44px)] leading-tight text-ink">
            The people behind the appointments.
          </h2>
        </div>
        <div class="space-y-5 text-[16px] leading-[1.85] text-muted">
          <p>
            Diana Morrison, RN provides injectables, IV hydration, and GLP-1 weight management. Amber Mingione, Licensed Esthetician provides Microneedling with the Procell Therapies device, topical PRF, Glo2Facial, dermaplaning, and BioRePeel as an add-on to an eligible service.
          </p>
          <p>
            Brandy, Licensed Esthetician provides facials, standalone BioRePeel, and facial waxing. Aundrea Pedigo, Licensed Esthetician provides wedding and special-event makeup, photo-shoot makeup, and permanent jewelry.
          </p>
          <p>
            An injectable appointment, a skin treatment, a brow wax, and wedding makeup ask for different expertise. The provider profiles make those differences visible before a client walks through the door.
          </p>
        </div>
      </div>
    </section>

    <section class="border-y border-walnut/10 bg-breath px-6 py-[clamp(56px,8vw,96px)]">
      <div class="mx-auto grid max-w-[1120px] gap-12 lg:grid-cols-[0.72fr_1.28fr] lg:gap-20">
        <div>
          <p class="kicker">Before you book</p>
          <h2 class="font-serif text-[clamp(30px,4vw,44px)] leading-tight text-ink">
            Browse the menu. Meet the people.
          </h2>
        </div>
        <div class="space-y-5 text-[16px] leading-[1.85] text-muted">
          <p>
            Service pages show the appointment details and booking path. The provider directory groups the work by person and licence type. For a question that does not fit either route, call or send a consultation inquiry.
          </p>
          <p>
            House of Rose includes medical, esthetic, and non-medical services, so who you see depends on the appointment. The provider profiles explain who does what in plain language.
          </p>
          <p>
            Joshua Shaw, MD is the practice’s medical director, providing medical direction and protocol supervision. He does not perform treatment appointments at House of Rose; the practitioner named for the service is the person associated with that appointment.
          </p>
          <p class="border-t border-walnut/15 pt-5 text-[13px] font-semibold tracking-[0.03em] text-muted-strong">
            Medical Director: Joshua Shaw, MD · FL Lic. ME136232
          </p>
          <div class="flex flex-wrap gap-x-8 gap-y-4 pt-2">
            <a href="/about/providers/" class="learn-more">Meet the team</a>
            <a href="/services/" class="learn-more">Browse services</a>
          </div>
        </div>
      </div>
    </section>

    <section class="px-6 py-[clamp(56px,8vw,96px)]">
      <div class="mx-auto grid max-w-[1280px] gap-px bg-walnut/15 lg:grid-cols-2">
        <a href="/about/hra/" class="group bg-bone p-[clamp(30px,5vw,58px)] transition-colors hover:bg-limestone/35">
          <p class="text-[12px] font-semibold uppercase tracking-[0.12em] text-gold">HRA</p>
          <h2 class="mt-4 font-serif text-[clamp(30px,4vw,48px)] leading-tight text-ink group-hover:text-olive">
            House of Rose Aesthetics
          </h2>
          <p class="mt-5 max-w-[520px] text-[15px] leading-[1.8] text-muted">
            See how each provider's role shapes the practice, and learn what to expect at the Punta Gorda location.
          </p>
          <span class="learn-more mt-8">About the practice <span aria-hidden="true">→</span></span>
        </a>

        <a href="/about/providers/" class="group bg-bone p-[clamp(30px,5vw,58px)] transition-colors hover:bg-limestone/35">
          <p class="text-[12px] font-semibold uppercase tracking-[0.12em] text-gold">Providers</p>
          <h2 class="mt-4 font-serif text-[clamp(30px,4vw,48px)] leading-tight text-ink group-hover:text-olive">
            Meet the team
          </h2>
          <p class="mt-5 max-w-[520px] text-[15px] leading-[1.8] text-muted">
            See each team member’s credentials, current work, and individual profile.
          </p>
          <ul class="mt-6 grid gap-2 text-[14px] leading-relaxed text-muted sm:grid-cols-2" aria-label="House of Rose team">
            {providers.map((provider) => (
              <li {...(sanityProviderIds.has(provider._id) ? sbObjectId(provider._id) : {})}>{provider.publicName}</li>
            ))}
          </ul>
          <span class="learn-more mt-8">View all providers <span aria-hidden="true">→</span></span>
        </a>
      </div>
    </section>
  </div>
</BaseLayout>

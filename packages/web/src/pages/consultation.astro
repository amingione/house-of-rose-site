---
import BaseLayout from '@/layouts/BaseLayout.astro';
import SchemaMarkup from '@/components/SchemaMarkup.astro';
import type { FAQ } from '@/lib/queries';
import {
  LOCAL_BUSINESS,
  breadcrumbList,
  faqPage,
  service as serviceJsonLd,
} from '@/lib/structuredData';
import { getHeroBanner } from '@/lib/heroBanners';

const site = Astro.site?.toString() ?? 'https://houseofrosefl.com/';
const canonicalURL = new URL(Astro.url.pathname, site);
const heroBanner = getHeroBanner('/consultation/');
const socialImage = new URL(heroBanner.src, site).toString();
const phoneDisplay = '(941) 400-0165';
const phoneHref = `tel:${LOCAL_BUSINESS.telephone}`;
const addressLine = `${LOCAL_BUSINESS.streetAddress}, ${LOCAL_BUSINESS.addressLocality}, ${LOCAL_BUSINESS.addressRegion} ${LOCAL_BUSINESS.postalCode}`;
const pageDescription =
  'Request an aesthetics consultation with House of Rose in Punta Gorda to discuss a treatment, concern, or short list of options.';

const faqs: FAQ[] = [
  {
    question: 'Do I need to know which treatment I want?',
    answer:
      'No. Share a treatment name if you have one. Otherwise, point to what catches your eye and say what you would—and would not—want to change. A shortlist is welcome; a decision is not required before the visit.',
  },
  {
    question: 'What happens during an aesthetics consultation?',
    answer:
      'You can describe what you notice, recent procedures, and how much recovery fits your calendar. Ask who performs a service, what recovery may involve, and how the relevant options differ. The purpose is to understand the tradeoffs before making a decision.',
  },
  {
    question: 'What happens after I submit the form?',
    answer:
      'House of Rose will contact you using the information you provide. Keep the form general; personal health details can wait for a private conversation with the practice.',
  },
  {
    question: 'When will I hear back after submitting the form?',
    answer:
      `Messages received Monday through Friday are typically answered within one business day. For faster help during practice hours, call ${phoneDisplay}.`,
  },
];

const serviceSchema = serviceJsonLd(
  {
    name: 'Aesthetics Consultation in Punta Gorda',
    description:
      'Consultations at House of Rose Aesthetics, a medical aesthetics practice in Punta Gorda, Florida.',
    url: canonicalURL.toString(),
    image: socialImage,
    serviceType: 'Medical aesthetics consultation',
  },
  site,
);

const breadcrumbSchema = breadcrumbList([
  { name: 'Home', item: site },
  { name: 'Consultation', item: canonicalURL.toString() },
]);

const faqSchema = faqPage(faqs);
---

<BaseLayout
  title="Aesthetics Consultation | Punta Gorda, FL"
  description={pageDescription}
  image={socialImage}
  imageAlt={heroBanner.alt}
  preloadImage={heroBanner.src}
  preloadImageSizes="(max-width: 1023px) 100vw, 53vw"
  chrome="landing"
>
  <SchemaMarkup item={serviceSchema} slot="head" />
  <SchemaMarkup item={breadcrumbSchema} slot="head" />
  {faqSchema && <SchemaMarkup item={faqSchema} slot="head" />}

  <header class="border-b border-gold-metal/25 bg-breath">
    <div
      class="mx-auto flex h-[76px] max-w-[1280px] items-center justify-between gap-5 px-5 sm:h-[88px] sm:px-8"
    >
      <a href="/" aria-label="House of Rose Aesthetics home">
        <img
          src="/logos/house-of-rose-monogram/hr-logo-gold.webp"
          alt="House of Rose Aesthetics"
          width="512"
          height="520"
          class="h-12 w-auto sm:h-14"
        />
      </a>
      <div class="flex items-center gap-5 text-[13px] sm:gap-7 sm:text-[15px]">
        <a href={phoneHref} class="border-b border-gold pb-1 font-serif text-ink hover:text-gold">
          Call <span class="hidden sm:inline">{phoneDisplay}</span><span class="sm:hidden">us</span>
        </a>
      </div>
    </div>
  </header>

  <div class="bg-breath pb-20 text-ink md:pb-0">
    <section class="border-b border-gold-metal/30">
      <div class="mx-auto grid max-w-[1440px] lg:min-h-[680px] lg:grid-cols-[0.95fr_1.05fr]">
        <div class="order-2 flex items-center px-5 py-12 sm:px-8 sm:py-16 lg:order-1 lg:py-20 lg:pl-[clamp(48px,6vw,96px)] lg:pr-12">
          <div class="max-w-[620px]">
            <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-gold sm:text-[12px]">
              Medical Aesthetics Practice · Punta Gorda
            </p>
            <h1 class="mt-5 font-display text-[clamp(48px,5.6vw,76px)] leading-[0.98] tracking-[-0.025em]">
              Bring us the question you actually have.
            </h1>
            <p class="mt-7 max-w-[560px] text-[17px] leading-[1.75] text-muted-strong sm:text-[18px]">
              You may have a treatment in mind, or only the change you keep noticing.
              We will compare the relevant options in plain language.
            </p>
            <div class="mt-8 flex flex-col gap-3 sm:flex-row">
              <a href="#consultation" class="btn-gold sm:min-w-[250px]">
                Request a consultation
              </a>
              <a href={phoneHref} class="btn-outline-gold sm:min-w-[190px]">
                Call {phoneDisplay}
              </a>
            </div>
            <p class="mt-7 flex items-start gap-2 text-[12px] leading-relaxed text-muted sm:text-[13px]">
              <svg class="mt-0.5 h-4 w-4 shrink-0 text-gold" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                <path d="M12 21s7-6.1 7-12a7 7 0 1 0-14 0c0 5.9 7 12 7 12Z" stroke="currentColor" stroke-width="1.6"/>
                <circle cx="12" cy="9" r="2.35" stroke="currentColor" stroke-width="1.6"/>
              </svg>
              <span>{addressLine} · Monday–Friday, 9 AM–5 PM</span>
            </p>
          </div>
        </div>

        <div class="hero-media relative order-1 min-h-[38svh] overflow-hidden bg-walnut sm:min-h-[46svh] lg:order-2 lg:min-h-full">
          <img
            src={heroBanner.src}
            sizes="(max-width: 1023px) 100vw, 53vw"
            alt={heroBanner.alt}
            width={heroBanner.width}
            height={heroBanner.height}
            loading="eager"
            decoding="async"
            fetchpriority="high"
            class="absolute inset-0 h-full w-full object-cover object-center"
          />
        </div>
      </div>
    </section>

    <nav aria-label="Other ways to begin" class="border-b border-gold-metal/30 bg-breath px-5 py-7 sm:px-8">
      <div class="mx-auto flex max-w-[1180px] flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
        <p class="text-[13px] leading-relaxed text-muted-strong">Explore treatment information before requesting a consultation.</p>
        <div class="flex flex-wrap gap-x-6 gap-y-3 text-[13px]">
          <a href="/services/" class="border-b border-gold text-ink hover:text-gold">Know the service? View services</a>
          <a href="/skin-analysis/" class="border-b border-gold text-ink hover:text-gold">Starting with a skin question? View skin analysis</a>
        </div>
      </div>
    </nav>

    <section data-consultation-purpose class="border-b border-gold-metal/30 px-5 py-[clamp(60px,8vw,96px)] sm:px-8">
      <div class="mx-auto max-w-[1180px]">
        <div class="grid gap-8 lg:grid-cols-[0.8fr_1.2fr] lg:gap-20">
          <div>
            <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-gold">
              What the time is for
            </p>
            <h2 class="mt-4 font-display text-[clamp(38px,5vw,58px)] leading-[1.04]">
              Ask the questions that depend on your goals, health history, timing, and treatment area.
            </h2>
          </div>
          <div class="max-w-[680px] text-[16px] leading-[1.85] text-muted-strong">
            <p>
              Use the consultation to compare two options, ask how noticeable a change may be, or talk through recovery and other practical considerations. You can also bring one direct question without asking for a larger plan.
            </p>
            <p class="mt-5">
              Bring a photograph, a short list, or a plain description of what catches your eye. Say what you want to leave alone, too.
            </p>
          </div>
        </div>

        <div class="mt-12 grid gap-px border border-gold-metal/35 bg-gold-metal/35 md:grid-cols-3">
          <article class="bg-breath p-7 sm:p-8">
            <h3 class="font-serif text-[23px] leading-tight">What you already know is enough.</h3>
            <p class="mt-4 text-[14px] leading-[1.75] text-muted-strong">
              If you know the service name, share it. Photographs and the names or dates of recent procedures can also help when they are relevant.
            </p>
          </article>
          <article class="bg-breath p-7 sm:p-8">
            <h3 class="font-serif text-[23px] leading-tight">Ask for the practical details.</h3>
            <p class="mt-4 text-[14px] leading-[1.75] text-muted-strong">
              Ask who performs the service and their licence type, what recovery can involve, and what other reasonable options exist.
            </p>
          </article>
          <article class="bg-breath p-7 sm:p-8">
            <h3 class="font-serif text-[23px] leading-tight">Keep the request form simple.</h3>
            <p class="mt-4 text-[14px] leading-[1.75] text-muted-strong">
              The form below asks only for your contact information and a general topic. House of Rose will contact you to arrange a time; personal health details can wait for the conversation at the practice.
            </p>
          </article>
        </div>
      </div>
    </section>

    <section id="consultation" class="scroll-mt-4 bg-bone px-5 py-[clamp(56px,7vw,88px)] sm:px-8">
      <div class="mx-auto grid max-w-[1180px] gap-10 lg:grid-cols-[0.72fr_1.28fr] lg:gap-16">
        <div class="lg:pt-5">
          <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-gold">
            Consultation request
          </p>
          <h2 id="consultation-heading" class="mt-4 font-display text-[clamp(40px,5vw,62px)] leading-[1.02] text-ink">
            Tell us how to reach you.
          </h2>
          <p class="mt-6 max-w-[440px] text-[15px] leading-[1.8] text-muted">
            Choose the closest topic—even “Help me choose.” We will contact you to
            arrange a time for the fuller conversation.
          </p>
          <div class="mt-8 border-t border-walnut/15 pt-7">
            <p class="font-serif text-[20px] text-ink">Prefer to speak first?</p>
            <div class="mt-3 flex flex-wrap gap-x-5 gap-y-2 text-[14px]">
              <a href={phoneHref} class="border-b border-gold text-ink hover:text-gold">
                Call {phoneDisplay}
              </a>
            </div>
          </div>
        </div>

        <form
          name="general-consultation"
          method="POST"
          action="/.netlify/functions/lead-submit"
          data-netlify="true"
          aria-labelledby="consultation-heading"
          aria-describedby="consultation-form-note"
          class="form-card bg-breath p-6 sm:p-8 lg:p-10"
        >
          <input type="hidden" name="form-name" value="general-consultation" />
          <input type="hidden" name="source-page" value="/consultation/" />
          <p class="hidden" aria-hidden="true">
            <label>Do not fill this out: <input name="bot-field" tabindex="-1" autocomplete="off" /></label>
          </p>
          <p id="consultation-form-note" class="mb-5 text-[12px] leading-relaxed text-muted">
            All fields are required.
          </p>

          <div class="grid gap-5 sm:grid-cols-2">
            <div>
              <label for="consultation-name" class="mb-2 block text-[13px] font-medium text-ink">
                Name
              </label>
              <input
                id="consultation-name"
                name="name"
                type="text"
                required
                autocomplete="name"
                placeholder="Your name"
                class="landing-input"
              />
            </div>
            <div>
              <label for="consultation-email" class="mb-2 block text-[13px] font-medium text-ink">
                Email
              </label>
              <input
                id="consultation-email"
                name="email"
                type="email"
                required
                autocomplete="email"
                inputmode="email"
                placeholder="Email address"
                class="landing-input"
              />
            </div>
            <div>
              <label for="consultation-phone" class="mb-2 block text-[13px] font-medium text-ink">
                Phone
              </label>
              <input
                id="consultation-phone"
                name="phone"
                type="tel"
                required
                autocomplete="tel"
                inputmode="tel"
                placeholder="Phone number"
                class="landing-input"
              />
            </div>
            <div>
              <label for="consultation-service" class="mb-2 block text-[13px] font-medium text-ink">
                What would you like to explore?
              </label>
              <select id="consultation-service" name="service-interest" required class="landing-input">
                <option value="Help me choose the right starting point">Help me choose</option>
                <option value="Advanced skin imaging">Advanced Skin Imaging &amp; Analysis</option>
                <option value="Botox, Daxxify, fillers, or injectable PRF">Botox, Daxxify, Fillers, or PRF</option>
                <option value="PRF or microneedling">PRF or Microneedling</option>
                <option value="Morpheus8, Forma, or Lumecca Peak">Morpheus8, Forma, or Lumecca Peak</option>
                <option value="Facials or chemical peels">Facials or Chemical Peels</option>
                <option value="IV hydration">IV Hydration</option>
                <option value="GLP-1 weight management">GLP-1 Weight Management</option>
              </select>
            </div>
          </div>

          <button type="submit" class="btn-gold mt-6 w-full">
            Request my consultation
          </button>
          <div class="mt-5 grid gap-2 border-t border-gold-metal/35 pt-5 text-[12px] leading-[1.6] text-muted sm:grid-cols-2">
            <p>Keep personal health details for a private conversation with the practice.</p>
            <p class="sm:text-right">
              Your information is used only to respond to your request.
              <a href="/privacy-policy/" class="border-b border-gold text-ink hover:text-gold">
                Privacy Policy
              </a>
            </p>
          </div>
        </form>
      </div>
    </section>

    <section class="px-5 py-[clamp(64px,8vw,108px)] sm:px-8">
      <div class="mx-auto grid max-w-[1180px] gap-12 lg:grid-cols-[0.72fr_1.28fr] lg:gap-20">
        <div>
          <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-gold">
            The practical questions
          </p>
          <h2 class="mt-4 font-display text-[clamp(40px,5vw,62px)] leading-[1.03]">
            Before your consultation
          </h2>
        </div>
        <div class="border-t border-gold-metal/50">
          {
            faqs.map((faq, index) => (
              <details class="faq-row group border-b border-gold-metal/50" open={index === 0}>
                <summary class="flex min-h-[72px] cursor-pointer list-none items-center gap-4 py-5 font-serif text-[19px] leading-snug sm:text-[21px]">
                  <svg class="h-5 w-5 shrink-0 text-gold transition-transform duration-200 group-open:rotate-45" viewBox="0 0 20 20" fill="none" aria-hidden="true">
                    <path d="M10 3v14M3 10h14" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/>
                  </svg>
                  <span data-visit-faq-question>{faq.question}</span>
                </summary>
                <p data-visit-faq-answer class="pb-7 pl-9 text-[15px] leading-[1.8] text-muted-strong">
                  {faq.answer}
                </p>
              </details>
            ))
          }
        </div>
      </div>
    </section>

  </div>

  <footer class="bg-breath px-5 py-10 sm:px-8">
    <div class="mx-auto grid max-w-[1180px] gap-8 border-t border-gold-metal/35 pt-9 md:grid-cols-[0.75fr_1fr_auto] md:items-center">
      <div>
        <p class="font-display text-[25px] tracking-[0.08em] text-gold">
          House of Rose
        </p>
        <p class="mt-1 text-[10px] uppercase tracking-[0.28em] text-muted">
          Aesthetics
        </p>
      </div>
      <address class="not-italic text-[12px] leading-[1.8] text-muted">
        {addressLine}<br />
        <a href={phoneHref} class="hover:text-gold">{phoneDisplay}</a>
      </address>
      <nav aria-label="Legal" class="flex gap-5 text-[12px] text-muted md:flex-col md:gap-2">
        <a href="/privacy-policy/" class="hover:text-gold">Privacy Policy</a>
        <a href="/terms-of-service/" class="hover:text-gold">Terms</a>
      </nav>
    </div>
  </footer>

  <div class="fixed inset-x-0 bottom-0 z-40 border-t border-gold-metal/40 bg-breath p-3 md:hidden">
    <a href="#consultation" class="btn-gold w-full shadow-lg">
      Request a consultation
    </a>
  </div>
</BaseLayout>

<style>
  .hero-media::after {
    content: '';
    position: absolute;
    inset: auto 0 0;
    height: 28%;
    pointer-events: none;
    background: linear-gradient(to top, rgba(20, 17, 15, 0.38), transparent);
  }

  .form-card {
    box-shadow: 0 28px 70px rgba(10, 8, 7, 0.22);
  }

  .landing-input {
    width: 100%;
    min-height: 52px;
    border: 1px solid rgba(122, 92, 42, 0.48);
    background: rgba(255, 255, 255, 0.68);
    padding: 0.8rem 0.95rem;
    color: #14110f;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    font-size: 0.9375rem;
    line-height: 1.4;
    transition: border-color 160ms ease, box-shadow 160ms ease, background-color 160ms ease;
  }

  .landing-input::placeholder {
    color: #6d6458;
    opacity: 1;
  }

  .landing-input:focus-visible {
    border-color: #7a5c2a;
    background: #fff;
    box-shadow: 0 0 0 3px rgba(122, 92, 42, 0.16);
    outline: 2px solid #7a5c2a;
    outline-offset: 2px;
  }

  .faq-row summary::-webkit-details-marker {
    display: none;
  }

  @media (min-width: 1024px) {
    .hero-media::after {
      inset: 0 auto 0 0;
      width: 16%;
      height: 100%;
      background: linear-gradient(to right, #f8f4ec 0%, rgba(248, 244, 236, 0.5) 38%, transparent 100%);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .faq-row svg,
    .landing-input {
      transition: none;
    }
  }
</style>
